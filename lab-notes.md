---
title: "Lab Notes"
permalink: /lab-notes/
eyebrow: "부록 · 날짜별 기록"
description: 실험·측정·결정의 날짜별 적립. 최신이 위.
---

## 2026-09-17

- **제출 서빙 조합 확정 — Core `claude-sonnet-5` · NPC `claude-haiku-4-5`**: 조합 확인
  self-play 5회차 1판(`305d77a6`) 완주. 점수 11.7→11.7→16.0→24.8→29.8(n=1, 순위 근거
  아님), 261.5초, 크래시·폴백 0, NPC 발화 100건 메타 표현 0건. planner가 5회차에
  `beats` 7개(상한 6)로 1회 재생성 후 복구, A.19와 같은 구조화 출력 상한 문제다.
- **판당 비용 재계산 (추정, 실측 토큰 아님)**: 하네스 기록의 호출 수·입출력 문자 × 단가.
  사람 5회차 완주 판(테스터6 `2338ec9f` 기준) **$0.68~0.97**, self-play 판 $0.54~0.78.
  비용의 87~94%가 Core이고 그중 관리자 검사가 Core 입력의 56~68%다. thinking 토큰·
  재생성·임베딩은 넣지 않았다(어댑터가 `response.usage`를 기록하지 않음).
- 제출 전까지 개발은 로컬 구성(Core `gemma4:12b` · NPC `kanana1.5:8b`)으로 계속한다.

## 2026-09-16

- **외부 API 서빙 평가 (Exp 1 부록 A.19)**: 로컬 채택 구성은 그대로 두고, 심사 기간
  서빙 제약(GPU 1장이라 동시 접속 1명, 홈서버 가용성) 때문에 외부 API를 **같은 러너·
  같은 게이트의 비열등 판정**으로 평가했다.
  - Core: PCA·극성·eval 게이트는 Sonnet 5(n=3 PCA 0.9667·eval 0.7143)와 Opus 5(n=1
    eval 0.5714, 경계값) 모두 통과. 지연 게이트는 Sonnet이 n=1과 n=3 사이에서 뒤집히는
    경계이고, Opus는 advisor p95가 로컬보다 38% 높아 미달.
  - NPC 의미 품질: Haiku 4.5·Sonnet 5 모두 구조 실패 0. 로컬 모델의 지속 약점이던
    "방금 들음 vs 기억" 구분을 둘 다 통과해 비열등 이상으로 판단(사람 검토 10케이스).
  - Gemini 무료 키는 **하루 20요청 상한**(80턴 중 14턴 성공)으로 NPC 후보에서 제외.
- **채점기 교체 `gemma3:12b` → `gemma4:12b`(think off)**: 16 GiB GPU에서 채점기와
  백엔드 모델이 스와핑하며 평가가 40분 넘게 멈춘 것이 계기. 같은 발화 20건 재채점에서
  두 채점기 일치 19/20(95%), 파싱 실패 0. 재측정한 7세 정책은 kanana·Haiku·Sonnet
  **전부 4/5 통과**. 1차(gemma3) Haiku 2/5·3/5는 VRAM 경합 잡음으로 판단.
  공통 약점은 항목 3(3턴 망각). Sonnet은 "유저가"라는 메타 표현 누출이 1건 있었다.
- **테스터6 사람 세션** (`2338ec9f`, 09:48~11:06): 5회차 완주. 점수 8.8→64.2→94.2×3,
  side_effect 셀은 끝까지 0. 피드백 11건을 DB와 대조했다. 추천 규칙이 이미 하는
  행동을 제안한 사례(F5)가 있었고, 신의 질문 15문답은 전부 `unknown`, 원숭이손 부작용은
  불분명했다(F11). 기구 수정 5건(F1·F3~F6)은 세션 뒤에 반영.
  → [Exp 2 케이스]({{ '/experiments/human-observations/' | relative_url }})
- **테스터7**: UI 피드백 2건(출력 텍스트 선택 방지, 장면에 없는 NPC 딤 처리)만
  기록했다. 판 완주·확정이 미확인이라 표본에 넣지 않는다.

## 2026-09-15

- **DB 세션 전수 판별 + RQ1 예비 집계**: attempt 248판을 문서 대조·셀프플레이 로그·
  행동 시그니처(이벤트 간격 CV) 3축으로 판별. 사람 세션 확정 5판 + 유력 1판(테스터2
  `e84af6ab`, 준비 문서의 사전 식별 규칙으로 사후 판별). 확정+유력 6판에서 규칙 20건:
  AI 계열 70%(원숭이손 수락 6/9 포함) vs 직접 작성 30%. 5회차 점수 하락은 판정 요동
  (동일 제출 67.9→53.3 문서화 사례)과 섞여 있어 경향 주장 보류.
- **개선 배치 3/3 이후 첫 실플레이 2판**: DB 이벤트 근거. 판 0468b70c 완주
  (원숭이손 제안 거절 · 신의 질문 미사용 · 11자 단문 제출 → 총점 8.8 fail, 최소 제출
  하한 실측), 판 806122f3 밤 제출 없이 이탈. 같은 날 attempts 121건 중 실플레이는
  2판. 로드 즉시 세션 생성이 빈 판을 다량 적립함을 확인.
- **NPC 모델 교체 `kanana` → `gemma4:12b`** (`think=off`, temperature 0.3): 정책
  `npc-dialogue-2` 기준 재평가. Gemma 41문답 실패·재생성 0, 중간값 2.599초·p95
  3.053초(초기 목표 3초 소폭 초과). 전언 출처 구분·행동 근거 답변의 정확성을 근거로
  교체하되, 최초 의미 품질 목표(중요 모순 0·관련성 90%)에는 미달임을 그대로 기록.
  `think=true`는 1024토큰 상한에서 3건 전부 답변 실패로 미적용.
- research.remakeday.com 사이트 개설: 리서치 허브 구조(옵션 B)로 스캐폴딩.

## 2026-09-14

- **Core 슬롯 `gemma4:12b-N` 채택**: Funnel Stage 0~4 실측 근거. 극성쌍(RM1) 통과는
  측정된 로컬 셀 중 유일.
- **NPC 슬롯 `kanana1.5:8b-q4km` 채택**: 상업 라이선스(Apache 2.0)가 결정 축.
  교체 전 루프 스모크 통과 (크래시 0 · 폴백 0/16).
- E6 formal: 정책 ON/OFF × 5모델 × n=5.

## 2026-09-13

- E7 설계 확정: 비대칭 요인 설계 9셀, Hard 게이트 C1~C13.
- Stage 0 (프로토콜 호환) · Stage 1 (smoke) 실측.
- `gemma4:e4b` 발견: 디스크 8.95 GiB, 실상주 3.06 GiB. 예산 판정을 실측 VRAM으로 전환.

## 2026-09-10

- 5-loop 종단간 게임플레이 검증.

## 2026-09-09

- **Tester 1 · Tester 2 사람 플레이 세션**: event-level DB 로그 보존.
  직접 작성 규칙의 의미 변형, 원숭이손 설명-저장 규칙 불일치 등 instrument 이슈 발견.
- Core `advisor_answer` 폴백 붕괴(EGR 0/12)와 코드 수정 후 회복(11/12) 기록.

## 2026-09-08

- 게임 리뷰 검증: DB 스냅샷 집계 (attempt 66 / loop 81 / 밤 제출 65, 통과율 3.1%).
  "채점기 문제" 등 리뷰 결론 일부는 근거 부족으로 기각.
